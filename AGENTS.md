# AGENTS.md

本文件约束所有在本仓库工作的 Codex/AI Agent。开始任何任务前，先阅读本文件和 `README.md`。如任务要求与本文件冲突，停止实现并向用户说明冲突，不得静默放宽约束。

## 1. 项目使命

构建一款面向中国大陆健康成年人的微信小程序：用户先设定健身目标，再制定一周训练计划，系统依据身体数据和每日训练负荷规划饮食；训练计划或实际完成度变化时，后续营养目标与食谱必须同步变化。

当前阶段是立项与设计。未经用户确认实施计划，不初始化框架、不安装依赖、不创建演示性空壳代码。

## 2. 决策优先级

发生取舍时按以下顺序决策：

1. 用户安全、隐私和过敏原约束。
2. 数值正确性、可追溯性和计划一致性。
3. 单人使用 Codex 的开发与维护便利性。
4. 无服务器运维和国内服务可用性。
5. MVP 交付速度。
6. 功能丰富度和生成内容的表现力。

## 3. 不可违反的产品约束

- 仅支持健康成年人，不实现疾病、孕期、未成年人、康复或医疗建议。
- `calculation-policy-v2` 的自动个性化能量目标只支持 18–45 岁、BMI `18.5–<24.0` 且完成最小化健康排除项确认的用户；其他用户不得得到自动热量盈亏。
- 产品流程必须是“身体档案/偏好 → 目标 → 训练计划 → 每日营养目标 → 食谱 → 执行反馈”。
- 规划周期为一周，允许按天调整。
- 训练计划使用结构化表单或文本解析，不做照片/截图识别。
- 只对现有食材提供拍照辅助识别；用户必须确认候选结果。
- 推荐结果支持有限对话修改，但所有修改必须转成经过校验的领域命令。
- 训练计划变更必须触发受影响日期的饮食重算。
- 过去的事实记录不得因新计划而改写。
- 用户锁定或手动修改过的餐单不得被后台静默覆盖。
- 热量和克数只由确定性代码产生，大模型不得作为数值来源。
- 过敏原永远是硬约束，不允许自动降级。
- 所有热量和运动消耗以估算值/区间呈现，不使用“精准”“医学级”等表述。

## 4. 已确认技术边界

- 客户端：微信原生小程序，TypeScript。
- 后端：腾讯云开发 CloudBase Node.js 云函数；不自行维护常驻服务器。
- 编排：LangGraph.js 单 Agent 状态图；MVP 禁止引入多 Agent。
- 模型：国内可用并完成相应备案的模型，首选通过 CloudBase AI+ 接入腾讯混元。
- 数据库：CloudBase 文档型数据库。
- 文件：CloudBase 私有云存储。
- 知识库：CloudBase AI+ 知识库，只放低频解释性内容。
- 营养：`NutritionProvider` 适配器 + 本地标准化缓存；TianAPI 是 MVP 候选，不得直接耦合。
- 食材识别：`VisionProvider` 适配器；首选混元视觉，必须保留手动录入降级路径。
- 动作库：仓库内版本化数据，不依赖运行时外部动作 API。
- 计算：仓库内 TypeScript 计算库，不调用 LLM 计算热量。

如需改变以上任一项，先编写 ADR，说明原因、替代方案、迁移和回滚策略，并取得用户确认。

## 5. 架构边界

实现时保持以下单向依赖：

```text
小程序页面
  → 云函数/API 控制器
    → 应用服务/领域命令
      → 领域模型与计算策略
      → Provider 接口
        → CloudBase、LLM、视觉、营养供应商适配器
```

- `domain` 不得导入 CloudBase SDK、LangGraph、模型 SDK 或供应商 DTO。
- `calculation` 必须是无网络、无数据库、可重复执行的纯 TypeScript 模块。
- `agent` 只能调用公开的应用服务工具，不得直接访问数据库集合。
- `providers` 负责鉴权、超时、重试、字段映射和供应商错误转换。
- 跨端 DTO 必须有运行时校验；优先采用 JSON Schema 或 Zod，并从同一来源生成 TypeScript 类型。
- 所有写操作包含用户身份、预期版本和幂等键。

## 6. 领域与版本规则

以下记录必须可追溯且不可原地覆盖历史版本：

- 身体档案版本。
- 健身目标版本。
- 训练计划版本。
- 每日营养目标版本。
- 饮食计划版本。
- 计算策略版本。
- 外部营养数据快照版本。

每个每日营养目标至少引用：

```text
userId
date
bodyProfileVersionId
goalVersionId
trainingPlanVersionId
energyPolicyVersion
nutritionPolicyVersion
createdAt
```

计划变更规则：

- 保存新训练计划时发出 `TrainingPlanChanged`。
- 只重算生效日期起的未来数据。
- 过去记录保持不变。
- 未锁定餐单可以生成新版本。
- 已锁定餐单只生成差异和待确认状态。
- 同一事件重复处理必须得到相同结果，不得产生多个有效版本。
- 新版本完整成功前，旧版本继续有效。

## 7. AI 使用规则

允许大模型：

- 意图识别和参数提取。
- 在缺少必要参数时提出澄清问题。
- 选择并调用白名单工具。
- 解释确定性计算结果和推荐理由。
- 在已验证食谱模板范围内生成自然语言烹饪说明。

禁止大模型：

- 自行编造热量、营养素、食材重量、动作 MET 或用户身体数据。
- 在缺少必需输入、可靠来源、适用人群证据或可行营养解时用默认值、模型推断或静默回退补齐。
- 绕过过敏原、忌口、目标范围或版本检查。
- 把未经校验的模型 JSON 直接写入数据库。
- 把用户健康数据写入公共知识库或跨用户向量记忆。
- 自主扩展到医疗诊断、伤病康复或极端节食建议。
- 执行任意代码、任意 URL、任意数据库查询或客户端提供的工具名称。

模型输出必须经过运行时 schema 校验。校验失败时最多做一次受控修复请求；再次失败则进入明确的降级路径。

## 8. 计算约束

- 当前确定性策略为 `calculation-policy-v2`。运行任何能量计算前必须验证年龄 18–45 岁、BMI `18.5–<24.0`、最小化健康排除项和必需输入；失败时返回 `unsupported_for_personalized_energy`，不得切换到其他公式。
- 在适用范围内，估算 BMR 使用 `14.52 × weightKg - 155.88 × sexCode + 565.79`，`sexCode` 男性为 `0`、女性为 `1`。公式性别变量只能由用户提供，不得由模型推断。
- 日常非训练 PAL 只评价不含显式训练的工作、通勤和家务，取轻 `1.50`、中 `1.75`、重 `2.00`；训练与 PAL 必须分开，禁止重复计入。
- 净训练消耗使用 `(MET - 1) × 3.5 × kg / 200 × 分钟`。MET 只能引用经人工审核的 2024 Adult Compendium 会话类别和来源代码，不得为单个力量动作编造 MET。
- 维持、减脂和增肌的初始能量调整分别为 `0%`、`-10%`、`+5%`。MVP 禁止自动扩大能量盈亏；训练计划改变只能通过确定性训练消耗差值改变受影响日期。
- 减脂目标不得使 BMI 低于 `18.5`。若可用体重记录显示每周下降超过 `0.5 kg`、增肌期每周增长超过当前体重 `0.5%`，或目标即将越过适用范围，停止继续调整并进入复核提示。
- 无计划训练时蛋白质采用成年人 RNI：男性 `65 g/日`、女性 `55 g/日`；一般运动或耐力训练为 `1.4 g/kg/日`；规律抗阻训练或增肌为 `1.6 g/kg/日`；自动推荐不得超过 `2.0 g/kg/日`。
- 脂肪默认中点为 `25%E` 且必须处于 `20%–30%E`，饱和脂肪必须 `<10%E`。
- 碳水化合物必须同时满足 `50%–65%E` 和至少 `120 g/日`，禁止单纯用剩余热量补足。
- 膳食纤维必须为 `25–30 g/日`，添加糖必须 `<10%E`；同时执行食物多样性、过敏原、忌口和食材可用量约束。
- 约束无解时返回 `nutrition_constraints_infeasible` 及结构化冲突原因，禁止放宽过敏原、健康边界或伪造食材与营养值。
- 所有常数集中在命名、版本化、带来源说明的策略对象中。策略至少包含 `policyVersion`、`sourceIds`、`applicableAgeRange`、`applicableBmiRange`、`effectiveDate` 和 `reviewedAt`。
- 内部统一使用克、千克、厘米、分钟和千卡；API 边界显式转换。
- 未经审核的供应商数据不得进入生产计算缓存。
- 营养汇总必须由食材每 100 克数据和实际克数复算，禁止信任供应商返回的汇总文本。

改变公式或常数时，必须增加策略版本和迁移说明，不得让历史计划悄然变化。

科学来源优先级为：中国现行标准和正式 DRIs、系统综述/荟萃分析、专业组织共识、适用人群明确的原始研究。博客、营销材料、模型回答和 API 供应商文案不得作为数值依据。README 的“科学证据登记”是当前来源清单；生产上线前必须复核《中国居民膳食营养素参考摄入量（2023 版）》正式表格，不能从二手网页转录常量。

## 9. 食材照片约束

- 图片上传到用户私有路径，不接受公开永久 URL 作为默认实现。
- 视觉模型只返回候选名称、置信度和可选属性，不自动确认克数。
- 候选名称必须映射到内部标准食材 ID。
- 用户确认前不得写入正式库存或触发营养计算。
- 原图默认在识别完成后 24 小时内删除；失败清理任务必须可重试。
- 日志不得包含 Base64 图片、临时签名 URL 或完整识别提示词中的个人信息。
- 识别失败时提供手动录入，不阻断核心流程。

## 10. 外部服务约束

所有外部服务都必须满足：

- 仅从云函数调用，密钥不得进入小程序包或仓库。
- 通过 Provider 接口隔离供应商 DTO。
- 明确超时、一次有限重试、熔断和降级行为。
- 记录供应商、请求 ID、延迟、状态和费用，不记录敏感请求正文。
- 契约测试使用脱敏 fixture；常规测试不得访问实时付费 API。
- 营养数据必须确认数据来源、商业授权、缓存许可和退出策略。
- 每条进入生产计算的营养和 MET 数据必须保留来源 ID、原始单位、食物状态或活动类别、版本及审核时间；来源链断裂时按无可靠数据降级。
- 模型必须满足中国大陆公开服务所需的备案/登记和内容标识要求。

任何“临时直连供应商”的实现都必须在同一变更中封装到适配器，禁止留下页面或领域层直连。

## 11. 隐私与安全约束

- 采用最小化收集；MVP 不收集详细病史、诊断、用药或精确位置，只保存启用个性化能量计算所需的最小化排除项确认及版本。
- 数据查询必须以服务端可信的用户身份限定范围，不信任客户端传入的 `userId`。
- 用户身体数据、计划和照片按私有数据处理。
- 提供查看、更正、导出和删除个人数据的能力设计。
- 敏感配置仅使用 CloudBase 密钥/环境配置；仓库只提交示例变量名。
- 错误和分析日志使用用户匿名标识，不记录完整身体档案或自由文本。
- 删除账户时同时安排数据库、对象存储、会话摘要和派生数据删除。
- 页面明确声明非医疗用途，并对伤病、疾病、进食障碍和极端目标停止建议。

## 12. 编码规范

- TypeScript 开启严格模式；禁止无理由的 `any`、非空断言和类型逃逸。
- 领域实体和策略使用清晰英文命名，用户文案和数据别名支持中文。
- 函数保持单一职责；把副作用留在边界层。
- 时间按 ISO 8601 存储，并保存业务时区；每日计划以用户业务日期而非服务器日期为准。
- 金额和营养数值避免隐式浮点比较；定义统一舍入规则。
- 不在提示词中复制可由工具读取的大段用户数据。
- 不新增未被当前需求使用的框架、数据库或基础设施。
- 不提交生成物、真实密钥、用户照片或含个人信息的 fixture。

## 13. 测试要求

每项功能至少覆盖其所在层的测试。以下测试不可省略：

- 公式、单位和舍入的单元测试。
- 年龄 `18/45`、BMI `18.5/24.0`、缺失输入和健康排除项的范围门禁测试。
- BMR 性别编码、PAL 不含显式训练、MET 来源 ID 和训练消耗区间测试。
- 维持/减脂/增肌初始值、禁止自动扩大盈亏和体重速度停止条件测试。
- 蛋白质分支、脂肪/碳水/纤维边界以及 `nutrition_constraints_infeasible` 测试。
- 目标/训练/饮食版本关联测试。
- 训练移动、取消、降低完成度后的影响范围测试。
- 重算任务幂等和失败回滚测试。
- 过敏原永不被放宽的属性测试。
- 食材汇总营养与显示结果一致性测试。
- 模型输出 schema 异常和供应商超时的降级测试。
- 图片候选确认门禁测试。
- 用户数据隔离和越权访问测试。

修复缺陷时先增加能复现问题的测试。不要以“模型通常会正确回答”作为测试依据。

## 14. 完成定义

只有同时满足以下条件才能声称任务完成：

- 行为符合 `README.md` 和本文件的产品约束。
- 新增逻辑有自动化测试，且相关测试真实运行通过。
- Lint、类型检查和构建通过。
- 没有真实密钥、敏感数据或未授权素材。
- 外部 API 具备超时、错误映射和降级路径。
- 数值结果可由结构化输入复算并追溯策略版本。
- 文档与实际行为一致。
- 完成前查看 `git diff`，确认没有覆盖用户无关改动。

若因为账号、供应商授权、备案或外部审批无法验证，明确列出未验证事项，不得声称已完成生产就绪。

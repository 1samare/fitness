# AGENTS.md

本文件约束所有在本仓库工作的 Codex/AI Agent。开始任务前依次阅读本文件、`README.md` 和 `DEVELOPMENT_PROGRESS.md`。任务与本文件冲突时必须停止并说明，不得静默放宽。

## 0. 分支与进度

- 固定在 `feat/v1.0` 开发，不创建新分支或工作区，除非用户明确改变。
- 开发前确认分支、工作区和 `DEVELOPMENT_PROGRESS.md` 当前阶段。
- `DEVELOPMENT_PROGRESS.md` 是唯一动态进度真源。
- 未真实运行并通过阶段要求的测试、lint、类型检查和构建，不得标记完成。
- 历史计划与规格只作审计依据。

## 1. 产品和交付边界

构建面向中国大陆健康成年人的本地优先 Web 内部测试应用。当前交付仅使用虚拟数据，最多 5 名已授权测试人员，非生产、非医疗。

固定流程：身体档案/偏好 -> 目标 -> 一周训练计划 -> 每日营养目标 -> 食谱 -> 执行反馈。

不支持疾病、孕期、未成年人、康复、进食障碍、极端节食或医疗建议。不得输入真实健康数据、真实个人照片、精确位置或其他可识别个人的信息。

## 2. 决策优先级

1. 用户安全、隐私和过敏原。
2. 数值正确性、可追溯性和版本一致性。
3. 本地数据完整性、备份恢复与可删除性。
4. 单人使用 Codex 的开发维护便利性。
5. 内部测试交付速度。
6. 功能丰富度和生成内容表现力。

## 3. 技术边界

- Web：React、TypeScript、Vite。
- 数据：IndexedDB + Dexie，仅当前浏览器 origin；不接远程数据库。
- 计算：仓库内纯 TypeScript，不调用 LLM 计算数值。
- Agent：LangGraph.js 单 Agent，助手路由动态加载；禁止多 Agent。
- AI：普通构建禁用；显式测试构建可直连一个 OpenAI-compatible 文本/视觉端点。
- 图片：只在内存处理，不写入 IndexedDB、备份、文件系统或对象存储。
- 测试数据：仓库内版本化合成 fixture，必须有 schema、版本、来源和 checksum。
- 部署：静态 Web 测试构建；无服务器、云函数、服务端身份、云存储或小程序运行时。

改变任一边界前先写 ADR，说明原因、替代方案、迁移与回滚，并取得用户确认。

## 4. 架构依赖

```text
React 页面
  -> 页面 Runtime
    -> 应用服务/领域命令
      -> 领域模型与确定性计算
      -> Provider 接口
        -> IndexedDB / 浏览器模型适配器
```

- `domain` 不得导入 Dexie、React、LangGraph 或供应商 DTO。
- `calculation` 必须无网络、无数据库、可重复。
- `agent` 只能调用公开应用服务工具。
- Provider 负责超时、有限重试、熔断、字段映射和安全错误。
- 跨边界数据必须运行时校验。
- 写操作包含预期版本和幂等键。

## 5. 领域与版本

身体档案、目标、训练计划、每日营养目标、餐单、计算策略和营养快照必须可追溯，不得原地覆盖历史版本。

训练计划保存后发出 `TrainingPlanChanged`；只重算生效日起的未来日期。过去事实不变。未锁定餐单可生成新版本；锁定或手改餐单只生成差异与待确认状态。同一事件重放不得产生多个有效版本，新版本完整成功前旧版本保持有效。

## 6. AI 规则

允许：意图识别、参数提取、必要澄清、白名单工具选择、解释确定性结果、在已验证模板内生成自然语言说明。

禁止：编造热量、营养、克数、MET 或身体数据；补齐缺失必需输入；绕过过敏原/范围/版本；把未经 schema 校验的 JSON 写入本地库；执行任意代码、URL、数据库查询或客户端提供的工具名。

输出必须通过严格 schema。失败最多受控修复一次，再失败必须进入明确的结构化或手动降级。

## 7. 计算约束

- 仅 `calculation-policy-v2`；年龄 18-45、BMI `18.5-<24.0`、健康排除项和必需输入缺一不可，否则 `unsupported_for_personalized_energy`。
- BMR：`14.52 x weightKg - 155.88 x sexCode + 565.79`；男性 `0`、女性 `1`，只能由用户提供。
- 非训练 PAL：轻 `1.50`、中 `1.75`、重 `2.00`；不得包含显式训练。
- 净训练消耗：`(MET - 1) x 3.5 x kg / 200 x minutes`；MET 仅用审核后的 2024 Adult Compendium 会话类别。
- 维持/减脂/增肌：`0%`、`-10%`、`+5%`；不得自动扩大盈亏。
- 减脂不得使 BMI 低于 `18.5`；异常体重速度或即将超范围时停止调整。
- 蛋白质按 RNI/`1.4`/`1.6 g/kg/day` 分支，自动推荐不超过 `2.0 g/kg/day`。
- 脂肪 `20%-30%E`，默认 `25%E`；饱和脂肪 `<10%E`。
- 碳水同时满足 `50%-65%E` 和至少 `120 g/day`。
- 纤维 `25-30 g/day`，添加糖 `<10%E`。
- 无解返回 `nutrition_constraints_infeasible` 和结构化冲突，绝不放宽过敏原或伪造数据。
- 常数集中在带 `policyVersion`、`sourceIds`、适用范围、生效日期和复核时间的策略对象。
- 营养汇总按每 100 克快照与实际克数复算。

## 8. 图片与模型测试

- 只接受 JPEG/PNG、最多 10 MiB。
- 浏览器内存解码和缩放；模型最多返回五个名称、置信度、食物状态候选。
- 候选必须映射内部测试食材；模型不得返回或决定克数。
- 用户明确选择候选并输入正整数克数前，不得写库存或触发营养计算。
- 失败时保留手动库存入口。
- 普通自动化只用本机固定桩，不访问实时付费模型。
- 真实测试模型仅用于用户显式授权的 preflight；不得记录 Key、正文或敏感数据。
- 测试构建结束立即撤销 Key，并删除私有环境文件、构建物和浏览器站点数据。

## 9. 本地隐私与数据权利

- 所有数据限定当前浏览器 origin，不声称跨设备同步或服务端恢复。
- 备份不得包含 API Key、原始图片、data URL 或临时对象 URL。
- 恢复要求 schema、数据集身份、版本、checksum 和明确覆盖确认；失败保持旧数据。
- 删除要求精确短语 `DELETE LOCAL DATA`，清除 IndexedDB、草稿、设置和助手恢复状态。
- 日志不得记录完整身体档案、自由文本、图片或模型请求正文。
- 普通构建必须证明无测试 Key。

## 10. 编码规范

- TypeScript strict；禁止无理由的 `any`、非空断言和类型逃逸。
- 函数单一职责，副作用留在边界层。
- 时间存 ISO 8601，并保存业务时区；每日计划按用户业务日期。
- 单位统一克、千克、厘米、分钟和千卡；API 边界显式转换。
- 不新增当前需求未使用的框架、数据库或基础设施。
- 不提交生成物、真实密钥、真实照片或个人信息 fixture。

## 11. 测试要求

至少覆盖：公式/单位/舍入；年龄与 BMI 边界；健康排除项；BMR/PAL/MET；能量调整与停止条件；营养边界与无解；版本关联；训练移动/取消/完成度；重算幂等和回滚；过敏原属性；营养汇总；模型 schema/超时降级；候选确认门禁；IndexedDB 原子恢复；删除；并发版本冲突。

缺陷先写复现测试。完成门禁：

```text
pnpm lint
pnpm typecheck
pnpm test
pnpm test:web
pnpm build:web
pnpm build:web:test
pnpm test:web:e2e
pnpm scan:web-secrets
pnpm scan:web-boundaries
git diff --check
```

只有全部真实通过、文档与行为一致、无真实密钥/个人数据/生成物，并明确维持“虚拟数据 Web 内测已交付，非生产”，才能声称完成。

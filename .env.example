# Copy these names to .env.test.local for an explicitly authorized test build.
# Never commit a real key or distribute a test build after the key is revoked.
VITE_TEST_LLM_BASE_URL=
VITE_TEST_LLM_API_KEY=
VITE_TEST_LLM_MODEL=

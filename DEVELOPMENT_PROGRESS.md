# DEVELOPMENT_PROGRESS

> 最后更新：2026-08-26
> 当前分支：`feat/v1.0`
> 当前状态：**虚拟数据 Web 内部测试版已交付（非生产）**

## 1. 交付结论

本轮重构已将原微信小程序 + CloudBase 方案收敛为本地优先 Web 测试应用。应用使用浏览器 IndexedDB 保存结构化数据，食材图片只在当前页面内存中短暂处理；测试模型密钥只允许通过未提交的 `.env.test.local` 注入，并且仅用于不超过 5 名内部测试者的虚拟数据测试。

本状态不代表生产就绪，不允许录入真实健康数据、真实身份信息、真实食材照片或其他敏感信息。

## 2. 阶段状态

| 阶段 | 状态 | 退出证据 |
| --- | --- | --- |
| 7W-0 冻结旧运行时与建立 Web 基线 | 已完成 | Web 工作区、测试与构建入口建立 |
| 7W-1 本地领域与确定性计算迁移 | 已完成 | 版本化计划、计算策略与领域测试保留 |
| 7W-2 IndexedDB 本地持久化 | 已完成 | 本地仓储、导入导出、并发版本门禁完成 |
| 7W-3 本地优先 Web 主流程 | 已完成 | 档案、目标、训练、营养、库存、餐单、执行反馈闭环 |
| 7W-4 浏览器内助手与受控工具 | 已完成 | 助手动态加载、运行时 schema 校验与显式降级 |
| 7W-5 测试模型接入边界 | 已完成 | 最小真实预检成功，密钥未进入源码或普通构建 |
| 7W-6 跨浏览器验收与旧平台移除 | 已完成 | Chromium/WebKit 4/4 E2E；旧小程序与 CloudBase 活动路径已移除 |

## 3. 已交付范围

- 本地优先 Web 主流程：身体档案/偏好 -> 目标 -> 训练计划 -> 每日营养目标 -> 食谱 -> 执行反馈。
- 训练计划变更后的未来日期重算、历史事实保护、锁定餐单待确认与幂等版本规则。
- 确定性 `calculation-policy-v2`、适用人群门禁、营养约束与来源追溯。
- IndexedDB 数据保存、备份导出、恢复导入、清空本地数据与两标签页陈旧写入冲突处理。
- 食材照片候选识别确认：原图不持久化，候选经用户确认后才进入正式库存。
- 浏览器内单 Agent 助手：仅调用白名单应用服务，运行时校验失败后进入手动录入降级。
- 助手运行时采用动态导入，普通首屏构建与助手大依赖分块。
- 旧 `miniprogram/`、`cloudfunctions/`、`cloudbase/`、CloudBase Provider/持久化代码、云端照片清理模型和相关配置已删除。
- README、ADR、设计规范、仓库约束和内部测试交接清单已同步为本地优先 Web 边界。

## 4. 验证记录

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| Lint | `pnpm lint` | 通过 |
| TypeScript | `pnpm typecheck` | 通过 |
| 全仓单元/集成测试 | `pnpm test` | 35 个测试文件、427 个测试通过 |
| Web 单元/集成测试 | `pnpm test:web` | 26 个测试文件、86 个测试通过 |
| 测试配置构建 | `pnpm build:web:test` | 通过；助手运行时独立动态分块 |
| 普通 Web 构建 | `pnpm build:web` | 通过；普通构建不携带测试密钥 |
| 跨浏览器 E2E | `pnpm test:web:e2e` | Chromium + WebKit 共 4/4 通过 |
| 密钥边界扫描 | `pnpm scan:web-secrets` | 80 个文件通过；已配置值未暴露 |
| 旧运行时边界扫描 | `pnpm scan:web-boundaries` | 190 个活动文件通过；旧路径不存在 |
| 真实模型最小预检 | `pnpm web:test:preflight` | `available`；224 ms；存在 request ID；127 tokens |
| Git 差异卫生 | `git diff --check` | 通过；无尾随空格或异常空行 |

E2E 覆盖首次启动、初始化、训练、营养、库存、餐单、训练完成、照片候选确认、备份/删除/恢复/再次删除，以及两标签页陈旧写入冲突和无效视觉模型 schema 的手动降级。

Vite 对大于 500 kB 的助手动态 chunk 给出性能提示，但未造成构建或功能门禁失败；该项作为后续性能优化观察项，不阻塞本次内部测试交付。

## 5. 测试数据与外部边界

- 仅使用虚拟数据，内部测试人数上限为 5 人。
- `.env.test.local` 不纳入版本控制；日志、扫描结果和文档不得输出密钥值。
- 普通构建是默认交付入口；测试模型构建只用于受控内部测试。
- 真实预检只证明测试时点的最小连通性，不构成供应商 SLA、生产备案、授权或长期可用性证明。
- 旧平台分支和历史资料只保留审计价值，不再作为活动运行时或后续开发入口。

## 6. 剩余人工事项

- 内部测试结束后立即吊销短期模型密钥，并删除本机未提交的 `.env.test.local`。
- 由不超过 5 名内部测试者按 `docs/testing/local-web-internal-test-handoff.md` 完成人工签收。
- 若未来进入真实用户或生产阶段，必须新建立项并重新完成隐私、备案、授权、威胁模型、部署与生产运维评审；不得直接沿用本测试版结论。

## 7. 当前退出条件

代码重构、自动化验证、跨浏览器验收、旧平台移除和交接文档均已进入本分支。除上述密钥吊销与人工签收外，本轮“本地优先 Web 虚拟数据内部测试版”开发范围无剩余代码项。
